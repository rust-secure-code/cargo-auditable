[package]
name = "baz"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate_type = ["cdylib", "lib"]

[dependencies]
foo = {path = "../foo", optional = true}
bar = {path = "../bar", optional = true}

[features]
default = ["foo"]
foo = ["dep:foo"]
bar = ["dep:bar"]

